[pytest]
# 从 subjects/ 目录运行: python -m pytest
testpaths = subject/backtest/tests scripts
python_files = test_*.py
python_functions = test_*
python_classes = Test*
# 不自动收集 test_deep_verification*.py (它们是脚本, 跑会全屏 print)
addopts = -q --tb=short
